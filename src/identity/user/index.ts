/**
 * Identity User Module
 * User profiles, metadata, preferences, status management
 *
 * Facade over src/user/ — original implementation location
 */

export { IdentityUserModule } from './user.module';
export { UserModule } from '../../user/user.module';
export { UserService } from '../../user/user.service';
